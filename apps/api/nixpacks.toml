[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install -g pnpm", "cd ../.. && pnpm install"]

[phases.build]
cmds = ["cd ../.. && pnpm build --filter api"]

[start]
cmd = "node dist/index.js"
